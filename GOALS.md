# Goals

## Functional requirements
Support piping from stdin for CI/CD workflows.
Parse TOML frontmatter and Markdown bodies into a unified structure.
Provide emitters for Gemini extensions (JSON), Claude code (tools), and Cursor (.mdc).
Support binary distribution via uvx, bunx, and standalone releases.
